---
title: "USDA-ERS Broadband Rural Impact"
output: 
  html_document:
    toc: yes
    toc_float: yes
    toc_depth: 4
---

```{r echo=FALSE, message=FALSE}
library(htmltools)
library(data.table)
library(DT)
source("../R/functions.R")
```

```{r echo=FALSE, results='asis', message=FALSE}

ds_info_files <- list.files(pattern = "*_00*.Rmd")

for (ds in ds_info_files) {
  rmd_yml <- extract_rmd_yml(ds)
  ds <- rmd_yml$dataset
  dt <- as.data.table(t(as.data.table(ds)), keep.rownames = T)
  colnames(dt) <- c("Item", "Value")
  print(h3(paste("Dataset:", dt[Item == "dataset_short_title", Value])))
  print(knitr::kable(dt[Item != "dataset_short_title",], col.names = c("", "")))
}
```

```{r dynamic_menus, include=FALSE}
section_number <- c("01", "02")
section_title <- c("INGEST", "STRUCTURE")
menu_sections <- data.frame(section_number, section_title)

m <- get_menu_title("dp_ds01_11.Rmd")

merge(menu_sections, m, by.x = "section_number", by.y = "menu_section")
```
